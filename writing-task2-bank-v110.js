import { registerRenderEnhancement } from './render-lifecycle-v15.js';

const CORE='ielts-self-learning-v1';
const ADAPTIVE='ielts-adaptive-v1';
const OWNER='WT2-BANK';
const CHECKS=[
  ['task','I answered every part of the exact task and maintained a clear position.'],
  ['position','My introduction gives a precise position or clear response to the required questions.'],
  ['development','Each main idea is explained before or alongside examples.'],
  ['organization','Paragraphs have distinct jobs and the progression is easy to follow.'],
  ['language','I checked recurring grammar, sentence-boundary and collocation problems.']
];
const p=(id,family,familyLabel,title,difficulty,prompt,requirements)=>({id,family,familyLabel,title,difficulty,prompt,requirements,source:{type:'original',label:'Original IELTS-style Task 2 practice'}});

export const WRITING_TASK2_PROMPTS=[
  p('WT2-OP-01','opinion','Opinion','Public transport or new roads',3,'Some people think governments should spend more money on improving public transport than on building new roads. To what extent do you agree or disagree?',['State how far you agree or disagree.','Keep the same position throughout the essay.','Develop reasons that directly compare the value of the two spending priorities.']),
  p('WT2-OP-02','opinion','Opinion','Community service at university',4,'University students should be required to complete a period of unpaid community service as part of their degree. To what extent do you agree or disagree?',['Take a clear position on whether the requirement should apply.','Address practical or educational consequences of making it compulsory.','Avoid discussing volunteering in general without answering the compulsory-degree requirement.']),
  p('WT2-DISC-01','discussion','Discussion + opinion','Online and classroom education',3,'Some people believe online education can replace most classroom teaching, while others think face-to-face learning will always be essential. Discuss both views and give your own opinion.',['Explain why online education could replace much classroom teaching.','Explain why face-to-face learning may remain essential.','Give and maintain your own judgement.']),
  p('WT2-DISC-02','discussion','Discussion + opinion','What museums should display',4,'Some people think museums should mainly present the history and culture of their local area, while others believe they should show objects and ideas from many parts of the world. Discuss both views and give your own opinion.',['Develop the case for a strong local focus.','Develop the case for wider international representation.','Make your own position explicit rather than ending with two balanced summaries only.']),
  p('WT2-ADV-01','advantages','Advantages / disadvantages','A four-day working week',3,'In some organisations, employees work four longer days instead of five standard days each week. Do the advantages of this arrangement outweigh the disadvantages?',['Identify meaningful advantages and disadvantages.','Compare their importance, not just their number.','State clearly which side is stronger overall.']),
  p('WT2-ADV-02','advantages','Advantages / disadvantages','Companies moving to regional areas',4,'Some governments encourage large companies to move offices and factories away from major cities to smaller towns and regional areas. Do the advantages of this policy outweigh the disadvantages?',['Discuss consequences for both major cities and receiving regions where relevant.','Explain the most important benefits and costs.','Make an explicit outweigh judgement.']),
  p('WT2-PS-01','problems-solutions','Problems / solutions','Long urban commuting times',3,'In many cities, people are spending more time travelling between home and work or study. What problems does this cause, and what measures could governments and employers take to reduce these problems?',['Explain problems caused by longer commuting time.','Propose measures that governments and/or employers can realistically take.','Link solutions to the problems instead of listing unrelated transport policies.']),
  p('WT2-PS-02','problems-solutions','Problems / solutions','Household food waste',4,'Large amounts of edible food are thrown away by households in many countries. Why does this happen, and what can be done to reduce household food waste?',['Explain plausible causes of household food waste.','Propose actions that address those causes.','Keep the focus on household waste rather than food loss during farming or manufacturing.']),
  p('WT2-TWO-01','two-part','Two-part question','Growth of second-hand shopping',3,'More people are buying second-hand clothes, furniture and electronic devices instead of purchasing new products. Why is second-hand shopping becoming more popular? Is this a positive or negative development?',['Answer why second-hand shopping is becoming more popular.','Make a clear positive/negative judgement.','Support the judgement with consequences, not only reasons for the trend.']),
  p('WT2-TWO-02','two-part','Two-part question','Car-free areas in city centres',4,'An increasing number of cities are creating car-free areas in their centres. Why are cities introducing these areas? How might this change affect residents and local businesses?',['Explain why cities are introducing car-free areas.','Explain effects on residents and local businesses.','Cover both questions clearly; do not turn the essay into a simple agree/disagree response.'])
];

let activeId=WRITING_TASK2_PROMPTS[0].id;
let opened=false;
let mode='practice';
let seconds=2400;
let timer=null;

const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const read=k=>{try{return JSON.parse(localStorage.getItem(k)||'{}')}catch{return {}}};
const write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
const words=(v='')=>(String(v).trim().match(/\S+/g)||[]).length;
const currentPrompt=()=>WRITING_TASK2_PROMPTS.find(x=>x.id===activeId)||WRITING_TASK2_PROMPTS[0];
function core(){const s=read(CORE);s.writingDrafts||={};s.notes||={};return s;}
function adaptive(){const s=read(ADAPTIVE);s.productiveEvidence||={writing:[],speaking:[]};s.productiveEvidence.writing||=[];s.productiveEvidence.speaking||=[];s.productivePriority||={};s.aiFeedbackReturns||={writing:[],speaking:[]};s.aiFeedbackReturns.writing||=[];s.aiFeedbackReturns.speaking||=[];return s;}
function evidenceRows(s,id){return (s.productiveEvidence.writing||[]).filter(x=>x.lessonId===OWNER&&x.blockId===id).sort((a,b)=>a.ts-b.ts);}
function feedbackRows(s,id){return (s.aiFeedbackReturns.writing||[]).filter(x=>x.lessonId===OWNER&&x.promptId===id).sort((a,b)=>a.ts-b.ts);}
function pendingFeedback(s,id){return [...feedbackRows(s,id)].reverse().find(x=>!x.appliedByEvidenceId)||null;}
function saveCore(id,key,value){const s=core();if(key==='draft')s.writingDrafts[id]=value;else s.notes[`WT2PLAN-${id}`]=value;write(CORE,s);}
function updatePriority(s){const rows=[...(s.productiveEvidence.writing||[])].sort((a,b)=>a.ts-b.ts);if(!rows.length){delete s.productivePriority.writing;return;}const retries=rows.filter(x=>x.attemptKind==='retry').length,recent=rows.slice(-4),average=recent.reduce((a,x)=>a+Number(x.score||0),0)/recent.length;s.productivePriority.writing={score:Math.min(1,(1-average)*.8+(retries===0?0.12:0)),attempts:rows.length,retries,average,latestAt:rows.at(-1)?.ts||null,lessonId:'W05',updatedAt:Date.now()};}
function compare(source,retry){const before=new Set(source?.criteria||[]),after=new Set(retry?.criteria||[]);return{processDelta:Number(retry?.score||0)-Number(source?.score||0),wordCountDelta:Number(retry?.wordCount||0)-Number(source?.wordCount||0),criteriaAdded:[...after].filter(x=>!before.has(x)),criteriaLost:[...before].filter(x=>!after.has(x))};}
function linkFeedback(s,retry){if(retry.attemptKind!=='retry')return;const f=[...feedbackRows(s,retry.blockId)].reverse().find(x=>!x.appliedByEvidenceId&&x.ts<=retry.ts);if(!f)return;const source=(s.productiveEvidence.writing||[]).find(x=>x.id===f.sourceEvidenceId);f.appliedByEvidenceId=retry.id;f.appliedAt=retry.ts;f.comparison=compare(source,retry);}
function coachingPrompt(prompt,draft){const target=core().profile?.targetBand||7;return `You are an IELTS Academic Writing learning coach. This is learning feedback, not an official IELTS score.\n\nTARGET: Band ${target} (learning target only)\nTASK TYPE: ${prompt.familyLabel}\n\nTASK:\n${prompt.prompt}\n\nMY RESPONSE:\n${draft}\n\nUse the public IELTS Academic Writing criteria: Task Response, Coherence and Cohesion, Lexical Resource, and Grammatical Range and Accuracy. Do not give a fake precise band score. Instead: identify my three highest-priority problems; check whether I answered every part of the task and maintained the required position or judgement; check idea development; identify organization and recurring language problems only when supported by my text; quote only short phrases when needed; give one repair action for each priority; then ask me to revise before showing a complete model answer.`;}
async function copy(text,button){try{await navigator.clipboard.writeText(text);const old=button.textContent;button.textContent='Copied';setTimeout(()=>button.textContent=old,1200)}catch{const a=document.createElement('textarea');a.value=text;document.body.appendChild(a);a.select();document.execCommand('copy');a.remove();}}
function clock(){return`${String(Math.floor(seconds/60)).padStart(2,'0')}:${String(seconds%60).padStart(2,'0')}`;}
function updateClock(){const e=document.querySelector('[data-wt2-timer]');if(e)e.textContent=clock();if(seconds===0){clearInterval(timer);timer=null;}}
function startTimer(){if(timer||seconds<=0)return;timer=setInterval(()=>{seconds=Math.max(0,seconds-1);updateClock();},1000);}
function pauseTimer(){clearInterval(timer);timer=null;}
function resetTimer(){pauseTimer();seconds=2400;updateClock();}
function statusHTML(s,prompt){const rows=feedbackRows(s,prompt.id),pending=pendingFeedback(s,prompt.id),latest=rows.at(-1);if(pending)return`<div class="callout warning" style="margin-top:12px"><strong>Feedback waiting for revision.</strong><br>${pending.priorities.map((x,i)=>`${i+1}. ${esc(x)}`).join('<br>')}<div class="small muted" style="margin-top:7px">Revise this same prompt, then save <strong>Revision / retry</strong> evidence.</div></div>`;if(latest?.appliedByEvidenceId){const d=latest.comparison?.processDelta||0;return`<div class="callout success" style="margin-top:12px"><strong>Feedback → retry cycle recorded.</strong><div class="small muted">Process self-check change ${d>=0?'+':''}${Math.round(d*100)} points.</div></div>`;}return'';}
function workspace(){const p=currentPrompt(),c=core(),s=adaptive(),draft=c.writingDrafts[p.id]||'',plan=c.notes[`WT2PLAN-${p.id}`]||'',n=words(draft),evidence=evidenceRows(s,p.id),latest=evidence.at(-1),pending=pendingFeedback(s,p.id),kind=pending?'retry':'first';return`<div data-wt2-workspace style="margin-top:18px"><div class="card elevated">
<div class="cluster" style="justify-content:space-between"><div><div class="eyebrow">Task 2 Practice Bank · V1.10</div><h2 style="margin-top:6px">10 full-length prompts across 5 task families</h2></div><span class="chip primary">Original practice</span></div>
<div class="grid two" style="margin-top:14px"><label class="stack"><strong>Practice prompt</strong><select class="text-input" data-wt2-select>${WRITING_TASK2_PROMPTS.map(x=>`<option value="${x.id}" ${x.id===p.id?'selected':''}>${esc(x.familyLabel)} · ${esc(x.title)}</option>`).join('')}</select></label><div><strong>Mode</strong><div class="cluster" style="margin-top:7px"><button class="btn ${mode==='practice'?'primary':'soft'}" data-wt2-mode="practice">Practice</button><button class="btn ${mode==='test'?'primary':'soft'}" data-wt2-mode="test">40-min Test</button><span class="chip" data-wt2-timer>${clock()}</span><button class="btn ghost small-btn" data-wt2-timer-action="start">Start</button><button class="btn ghost small-btn" data-wt2-timer-action="pause">Pause</button><button class="btn ghost small-btn" data-wt2-timer-action="reset">Reset</button></div></div></div>
<div style="margin-top:18px"><div class="eyebrow">${esc(p.familyLabel)} · ${p.id} · Difficulty ${p.difficulty}/5</div><h3 style="margin-top:6px">${esc(p.title)}</h3><div class="reading-passage" style="margin-top:10px"><p>${esc(p.prompt)}</p></div></div>
<div class="grid two" style="margin-top:16px"><label class="stack"><strong>My plan</strong><textarea class="text-area" data-wt2-plan placeholder="Position / answer to each task part…\nBody 1 idea + development…\nBody 2 idea + development…">${esc(plan)}</textarea></label><div class="card subtle"><strong>Task checklist</strong>${mode==='test'?'<p class="small muted" style="margin-top:8px">Hidden in Test Mode. Complete the timed attempt before reviewing task requirements.</p>':`<ul style="margin-top:8px">${p.requirements.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`}</div></div>
<div style="margin-top:16px"><div class="cluster" style="justify-content:space-between"><div><strong>Task 2 response</strong><div class="small muted">Aim for at least 250 words. Drafts use the existing local writing store.</div></div><span class="chip ${n>=250?'success':'warning'}" data-wt2-count>${n} words</span></div><textarea class="text-area workspace-editor writing-input" style="margin-top:8px" data-wt2-draft data-writing-id="${p.id}" placeholder="Write your complete Task 2 response here…">${esc(draft)}</textarea></div>
<section class="card subtle" style="margin-top:16px"><div class="eyebrow">Productive evidence</div><div class="grid two" style="margin-top:10px"><label class="stack"><strong>Attempt type</strong><select class="text-input" data-wt2-kind><option value="first" ${kind==='first'?'selected':''}>First attempt</option><option value="retry" ${kind==='retry'?'selected':''}>Revision / retry</option></select></label><div><strong>Evidence so far</strong><p class="small muted" style="margin-top:7px">${evidence.length} saved attempt${evidence.length===1?'':'s'}${latest?` · latest ${latest.wordCount} words · self-check ${Math.round((latest.score||0)*100)}%`:''}</p></div></div><div class="checklist" style="margin-top:12px">${CHECKS.map(([id,label])=>`<label class="check-item"><input type="checkbox" data-wt2-criterion="${id}"> ${esc(label)}</label>`).join('')}</div><div class="cluster" style="margin-top:12px"><button class="btn primary" data-wt2-save-evidence ${n>=250?'':'disabled'}>${pending?'Save revision / retry evidence':'Save attempt evidence'}</button><span class="small muted">250+ words required; this is process evidence, not an IELTS band.</span></div></section>
<section class="card subtle" style="margin-top:16px"><div class="eyebrow">AI feedback → revision</div><p class="muted" style="margin-top:7px">Copy coaching feedback only after your own attempt. Return with 2–3 actionable priorities, then revise this same prompt.</p><button class="btn ${mode==='test'?'soft':'primary'}" data-wt2-copy ${mode==='test'?'disabled':''}>${mode==='test'?'AI coaching hidden in Test Mode':'Copy AI coaching prompt'}</button><div class="stack" style="margin-top:12px"><label><strong>Priority 1</strong><input class="text-input" data-wt2-feedback maxlength="220" placeholder="One concrete change from feedback"></label><label><strong>Priority 2</strong><input class="text-input" data-wt2-feedback maxlength="220" placeholder="A second concrete change"></label><label><strong>Priority 3 <span class="small muted">optional</span></strong><input class="text-input" data-wt2-feedback maxlength="220" placeholder="Optional third change"></label></div><div class="cluster" style="margin-top:12px"><button class="btn soft" data-wt2-save-feedback ${latest&&!pending?'':'disabled'}>Save feedback priorities</button><span class="small muted">Save attempt evidence first. External AI scores are not imported.</span></div>${statusHTML(s,p)}</section>
<div class="small muted" style="margin-top:12px">Source: ${esc(p.source.label)}. Not an official IELTS question and not affiliated with IELTS.</div></div></div>`;}
function bank(){return`<section class="focus-card" data-wt2-bank-card style="margin-bottom:18px"><div class="eyebrow">Academic Writing · Task 2</div><h2>Practice breadth after W05: 5 task families × 2 full essays</h2><p class="lede">Transfer W01–W05 skills across Opinion, Discussion, Advantages/Disadvantages, Problems/Solutions and Two-part questions.</p><div class="cluster"><button class="btn primary" data-wt2-open>${opened?'Close Task 2 Practice Bank':'Open Task 2 Practice Bank'}</button><button class="btn soft" data-lesson="W05">Review the full Task 2 workflow first</button></div>${opened?workspace():''}</section>`;}
function mount(){if(!location.hash.includes('/ielts')||document.querySelector('[data-wt2-bank-card]'))return;const anchor=document.querySelector('[data-wt1-ielts-card]')||document.querySelector('#main .page-head');anchor?.insertAdjacentHTML('afterend',bank());}
function rerender(){const old=document.querySelector('[data-wt2-bank-card]');if(old)old.outerHTML=bank();else mount();}
function saveEvidence(){const p=currentPrompt(),c=core(),draft=c.writingDrafts[p.id]||'',n=words(draft);if(n<250)return;const card=document.querySelector('[data-wt2-workspace]'),criteria=[...card.querySelectorAll('[data-wt2-criterion]')].filter(x=>x.checked).map(x=>x.dataset.wt2Criterion),attemptKind=card.querySelector('[data-wt2-kind]')?.value||'first',s=adaptive(),event={id:`pe-${Date.now()}-${p.id}`,ts:Date.now(),skill:'writing',lessonId:OWNER,blockId:p.id,promptId:p.id,attemptKind,criteria,score:criteria.length/CHECKS.length,wordCount:n};s.productiveEvidence.writing.push(event);linkFeedback(s,event);updatePriority(s);write(ADAPTIVE,s);window.dispatchEvent(new CustomEvent('ielts-productive-evidence-change',{detail:event}));rerender();}
function saveFeedback(){const p=currentPrompt(),s=adaptive(),source=evidenceRows(s,p.id).at(-1);if(!source||pendingFeedback(s,p.id))return;const priorities=[...document.querySelectorAll('[data-wt2-feedback]')].map(x=>x.value.trim()).filter(Boolean).slice(0,3);if(priorities.length<2){alert('Add at least two actionable feedback priorities.');return;}const event={id:`afr-${Date.now()}-${p.id}`,ts:Date.now(),skill:'writing',lessonId:OWNER,promptId:p.id,sourceEvidenceId:source.id,sourceAttemptKind:source.attemptKind,priorities,appliedByEvidenceId:null,appliedAt:null,comparison:null};s.aiFeedbackReturns.writing.push(event);write(ADAPTIVE,s);window.dispatchEvent(new CustomEvent('ielts-ai-feedback-return-change',{detail:event}));rerender();}

document.addEventListener('input',e=>{if(e.target.matches('[data-wt2-draft]')){saveCore(activeId,'draft',e.target.value);const n=words(e.target.value),count=document.querySelector('[data-wt2-count]'),button=document.querySelector('[data-wt2-save-evidence]');if(count){count.textContent=`${n} words`;count.className=`chip ${n>=250?'success':'warning'}`;}if(button)button.disabled=n<250;}else if(e.target.matches('[data-wt2-plan]'))saveCore(activeId,'plan',e.target.value);});
document.addEventListener('change',e=>{if(e.target.matches('[data-wt2-select]')){activeId=e.target.value;resetTimer();rerender();}});
document.addEventListener('click',e=>{const open=e.target.closest('[data-wt2-open]');if(open){opened=!opened;pauseTimer();rerender();return;}const m=e.target.closest('[data-wt2-mode]');if(m){mode=m.dataset.wt2Mode;if(mode==='practice')pauseTimer();else resetTimer();rerender();return;}const t=e.target.closest('[data-wt2-timer-action]');if(t){if(t.dataset.wt2TimerAction==='start')startTimer();if(t.dataset.wt2TimerAction==='pause')pauseTimer();if(t.dataset.wt2TimerAction==='reset')resetTimer();return;}const cp=e.target.closest('[data-wt2-copy]');if(cp&&!cp.disabled){const p=currentPrompt(),draft=core().writingDrafts[p.id]||'';copy(coachingPrompt(p,draft),cp);return;}if(e.target.closest('[data-wt2-save-evidence]')){saveEvidence();return;}if(e.target.closest('[data-wt2-save-feedback]'))saveFeedback();});
window.addEventListener('ielts-productive-evidence-change',()=>setTimeout(()=>{if(opened&&location.hash.includes('/ielts'))rerender();},0));
window.addEventListener('ielts-ai-feedback-return-change',()=>setTimeout(()=>{if(opened&&location.hash.includes('/ielts'))rerender();},0));
registerRenderEnhancement(mount);

export { coachingPrompt, words };
