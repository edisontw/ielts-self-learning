# V1.3 Learner-Journey E2E QA

Date: 2026-08-26
Baseline: `main` after Quick Placement + L01–L05 production-audio closure.
Production closure commit after QA fix: `e0562d708c8e30eaf299a96c6b234f1c44852114`.
Deployed site: `https://edisontw.github.io/ielts-self-learning/`

## Goal

Verify one real learning loop:

`Placement → Study Plan / Today → lesson → Lab / Mini Test → Error Notebook → Repair → Retry → review → Full Mock → backup / restore`

## Automated coverage

- Placement and prerequisite-safe adaptive routing.
- Weak Mini Test evidence rebalances Study Plan priorities.
- Wrong standard-lesson questions can be unlocked and retried.
- Saved lesson errors stay in Error Notebook during retry and close automatically only after a checked correct answer.
- Mini Test / Full Mock errors do not expose an invalid `#/lesson/<test-id>` single-question retry route.
- Repair guided-practice items can be retried after a wrong check.
- Repair completion is blocked until every guided-practice item is checked and correct.
- Placement + L01–L05 production-first audio and labelled browser-voice fallback remain covered.
- Full Mock structure, Error Notebook handoff and history are covered.
- Placement, Study Plan, Error Notebook, Repair evidence, Mini Test history, Full Mock history, productive evidence and AI feedback survive backup/restore.
- Restored state remains healthy in Diagnostics.
- GitHub Actions browser smoke covers core desktop routes plus mobile IELTS / Full Mock shell.

## Defects found and fixed

### E2E-01 — Practice/Repair retry loop was not executable

Before this audit, a checked wrong standard lesson answer became permanently disabled even though Practice Mode and Error Notebook told the learner to retry. Repair lesson questions behaved the same way, and a Repair lesson could be marked complete without completing guided practice.

Fix:
- add `Retry` to wrong Practice Mode lesson questions;
- add `Retry question` from Error Notebook for standard lesson/Lab sources;
- close a saved lesson error automatically only after a successful checked retry;
- keep Mini Test / Full Mock errors on test-level retry workflows;
- add Repair-question retry;
- require all Repair guided-practice answers to be checked and correct before completion.

Regression: `tests/validate-repair-retry-loop.mjs` plus expanded `tests/qa-learner-journey.mjs`.

### E2E-02 — Deployed VG Repair route crashed after E2E-01 merge

Manual deployed QA found `#/lesson/VG01` rendering `Lesson not found` with:

`ReferenceError: repairReadyToComplete is not defined`

Cause: `learning-runtime-v3.js` used the Repair completion/retry helpers as runtime bindings, but the helper module did not expose those bindings to the deployed runtime.

Fix:
- expose `repairReadyToComplete` and `resetRepairAnswer` as explicit runtime bindings in `repair-retry-v1.js`;
- add CI regression assertions for the deployed Repair runtime bindings.

Fix PR: #24 — `Fix deployed VG Repair runtime bindings`
Merged commit: `e0562d708c8e30eaf299a96c6b234f1c44852114`
Post-merge CI: PASS.
Post-merge GitHub Pages deployment: PASS.
Post-deploy VG01 E2E: PASS.

## Manual deployed QA closure

Final deployed Chromium run: GitHub Actions run `32976628196` (`V1.3 deployed E2E QA`, run #8).
Artifact: `v13-deployed-e2e`, artifact ID `9609776360`.
Result: **6 / 6 PASS** with no browser page errors.

- [x] **1. Lesson retry loop** — Desktop lesson wrong → Save error → Retry → correct → Improve shows `Corrected`.
- [x] **2. VG Repair mastery gate** — intentionally wrong → Retry → correct; Finish remains blocked until every guided-practice item is checked and correct.
- [x] **3. Mobile 390 px Error Notebook** — Retry returns to the correct lesson, no horizontal overflow, mobile navigation remains usable, and the retried error resolves after a correct checked answer.
- [x] **4. Placement + L01–L05 production audio / fallback** — all six deployed MP3s load and start from GitHub Pages with `readyState=4`; forced L05 MP3 failure exposes the labelled browser-voice fallback.
- [x] **5. Mini Test + Full Mock Test Mode / Error Notebook handoff** — Test Mode restrictions remain hidden before submission; MR01 and Full Mock missed objective items reach Error Notebook without exposing fake single-question lesson retry routes.
- [x] **6. Export → reset → import** — reset clears learner evidence, reload may recreate only an empty adaptive-state shell, theme is preserved, and import restores the exported learner state exactly.

### Audio evidence from final run

| Surface | Deployed asset | Bytes | Browser readiness |
| --- | --- | ---: | ---: |
| Quick Placement | `media/audio/placement-listening.mp3` | 1,894,025 | 4 |
| L01 | `media/audio/l01-listen-for-meaning.mp3` | 1,470,841 | 4 |
| L02 | `media/audio/l02-connected-speech.mp3` | 772,431 | 4 |
| L03 | `media/audio/l03-listening-paraphrase.mp3` | 716,007 | 4 |
| L04 | `media/audio/l04-distractors.mp3` | 1,091,543 | 4 |
| L05 | `media/audio/l05-predict.mp3` | 663,971 | 4 |

Forced production-audio failure displayed both:
- `Production audio unavailable`
- `Browser voice fallback · not production IELTS audio`

### Reset / restore evidence

After Reset + reload, storage was:

- core learner state: removed;
- Study Plan: removed;
- Full Mock state: removed;
- adaptive state: recreated only as an empty shell (`reviewSchedule`, `repairProgress`, histories and skill performance empty);
- theme: preserved according to the user's current appearance preference.

Import then restored and reverified:
- Quick Placement profile;
- corrected lesson errors;
- VG01 Repair completion;
- MR01 Mini Test history;
- Full Mock history;
- 8-week Study Plan;
- the exported portable learner-state payload.

## Non-blocking polish note

At 390 px, the Error Notebook `Retry question` button measured about `130 × 40 px`. It exceeds the WCAG 2.2 minimum target size used for this QA and is easy to tap, but its 40 px height is below a conservative 44 px mobile comfort target. Treat as future UI polish, not a V1.3 release blocker.

## Closure

**V1.3 production learner-journey E2E QA: PASS.**

Release-blocking defects found during deployed QA were fixed, merged, deployed and reverified. No remaining V1.3 E2E blocker was found in the six required production scenarios.
