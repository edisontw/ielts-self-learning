# V1.6 — Low-Frequency Test/Mock Transfer Audit Closure

Date: 2026-08-29
Status: **AUDIT-CLOSED**
Baseline main: `c78ae683adf461bfd1bddb6213678ac57617e564`

## Goal

Close the remaining 1–2 signal Reading/Listening Test/Mock semantic backlog without manufacturing routes, Repair lessons, or cosmetic retags from sparse evidence.

The gate is semantic, not frequency-only:

- split a tag only when it collapses meaningfully different cognitive processes;
- preserve a tag when the existing `skill` field already makes it unambiguous;
- add a route only when repeated transfer evidence and an exact existing practice destination make the CTA useful;
- add Repair only when a recurring, coherent skill gap remains genuinely untreated.

## Completed semantic corrections

Three low-frequency cases required real semantic intervention before this closure:

1. **Listening sequence umbrella**
   - `ML02-Q4`, `ML03-Q4` → `listening-spatial-sequence`
   - `ML04-Q3` → `listening-procedural-sequence`
   - spatial sequence is taught by QL03; procedural sequence remains a one-off signal;
   - no LR02.

2. **Reading definition umbrella**
   - `MR02-Q4` → `reading-explicit-definition`
   - `MR04-Q4` → `reading-distinction`
   - the original shared `reading-definition` label was heterogeneous and would have created false two-form recurrence.

3. **Listening final meaning**
   - `ML02-Q9`, `ML04-Q10` → `listening-conditional-outcome`
   - L04 is the exact teaching owner for conditional option → FINAL meaning;
   - the family remains two-form evidence only, with no runtime CTA or LR Repair.

## Remaining one-off / two-off Test/Mock decisions

### MA01-L01 — `spelling`

Decision: **KEEP-SKILL-AWARE / WATCH-OWNER**

- one Full Mock Listening signal;
- QL02 explicitly owns Listening spelling/form accuracy;
- `spelling` is already semantically precise when paired with `skill: listening`;
- adding a cosmetic `listening-` prefix would not improve routing or diagnosis;
- no existing-practice CTA or Repair from one signal.

### MA01-L31 — `definition`

Decision: **KEEP-SKILL-AWARE / OBSERVE**

- one Full Mock Listening signal;
- the question is a direct explicit-definition retrieval item;
- there is currently no exact Listening definition teaching/error owner;
- one item is not enough evidence for a new Lab, route, or Repair;
- `definition` is unambiguous within the Listening skill context, so no cosmetic retag is required.

### MA01-L33 + MA01-R24 — `academic-vocabulary`

Decision: **KEEP-SPLIT-BY-SKILL / OBSERVE**

- one Listening signal + one Reading signal;
- this is not a two-signal same-skill recurrence;
- skill-aware analysis must keep the two separate even though the textual error tag matches;
- aggregate count alone must never trigger a route or Repair.

## Closed discovery gate

After the sequence, Reading-definition, and conditional-outcome semantic corrections, all remaining 1–2 signal Test/Mock Reading/Listening families are now accounted for.

No additional runtime change is justified in this closure:

- no new localStorage key or schema;
- no raw question-bank migration;
- no new existing-practice route;
- no new RR/LR Repair;
- no fake Test/Mock single-question Retry;
- no progress-denominator change.

## Regression contract

`tests/audit-low-frequency-transfer-v16.mjs` now locks the following decisions:

- heterogeneous Reading definition signals stay split;
- conditional-outcome stays a genuine two-form watch family with L04 owner and no runtime ownership;
- Full Mock `spelling` remains one skill-aware signal with QL02 as the known owner;
- Full Mock `definition` remains one skill-aware observation;
- `academic-vocabulary` remains one signal per skill rather than a same-skill recurrence;
- none of the remaining sparse signals gains route/Repair ownership.

## Next gate

V1.6 semantic-gap discovery is complete enough to stop mining sparse tags for interventions.

The next higher-value step should be **new independent transfer evidence**, not more route invention from the existing single Full Mock. The preferred V1.7 direction is a second independent Full Academic Mock (`MA02`) or equivalent new Test/Mock evidence set. New evidence can then determine whether currently sparse skills such as Listening spelling, definition retrieval, conditional outcome, and procedural sequence actually recur.

Any future third independent same-skill signal should trigger a fresh semantic/route-utility review before a new Repair is considered.
