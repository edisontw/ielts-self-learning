# V1.12 MA02 Production Listening Audio — Preparation Checkpoint

**Status:** PREP-CLOSED / AUDIO-ASSETS-PENDING  
**Date:** 2026-08-30

## What is complete

PR #77 locked the production preparation layer for MA02 Listening without changing the live audio source.

Merged main:

`8a52fddebcffbebe16eed4dfe89172ada75cabfe`

Validation:

- PR Validate #414: PASS;
- main Validate #415: PASS;
- Pages #244 build/deploy/report: PASS;
- V1.12 MA02 production-audio preparation guard: PASS;
- existing MA02 browser interaction: PASS;
- existing deployed V1.7 / V1.9 / V1.10 / V1.11 E2E gates: PASS.

## Locked source and production plan

Source of truth remains:

`mock-test-data-v2.js`

Production preparation files:

- `media/audio/mock-tests/ma02-production-audio-spec-v1.json`
- `media/audio/mock-tests/MA02-PRODUCTION-AUDIO-PLAN.md`
- `tests/validate-ma02-audio-prep-v112.mjs`

The validation guard reconstructs every production segment and requires exact equality with the current MA02 source scripts.

## Canonical required MP3 files

The next operator/media step is to create or upload exactly these four files:

1. `media/audio/mock-tests/ma02-listening-part1-printmaking-workshop-booking.mp3`
2. `media/audio/mock-tests/ma02-listening-part2-observatory-visitor-orientation.mp3`
3. `media/audio/mock-tests/ma02-listening-part3-local-history-digitisation-project.mp3`
4. `media/audio/mock-tests/ma02-listening-part4-seed-banks-seed-storage.mp3`

Target structure:

| Part | Structure | Script words | Target duration |
|---|---|---:|---:|
| 1 | Receptionist + Caller | 204 | 90–105 s |
| 2 | Guide monologue | 257 | 110–125 s |
| 3 | Tutor + Nina + Omar | 256 | 110–125 s |
| 4 | Lecturer monologue | 307 | 128–145 s |

The exact speaker direction, turn segmentation, pace, pause rules and answer-bearing QA are already locked in the production plan/spec. Do not rewrite them in a later window.

## Current live behavior — intentionally unchanged

Until all four MP3 files exist and pass QA:

- `MA02.audioStatus` remains `browser-voice-gate`;
- `MOCK_AUDIO.MA02` remains four empty strings;
- UI remains labelled `Browser voice beta`;
- browser speech synthesis is the active MA02 source;
- one-play Test Mode semantics remain unchanged.

Do **not** wire nonexistent or unapproved paths just to make MA02 appear production-ready.

## Integration sequence after the four MP3s arrive

1. Verify each recording against the locked exact script and `criticalQa` list.
2. Inspect duration, channels, sample rate, bitrate/loudness and clipping.
3. Normalize only if needed; do not alter wording or answer timing.
4. Compute SHA-256 and exact file size.
5. Add provenance, format, duration, checksum and status to `media/audio/manifest-v1.json`.
6. Wire all four canonical paths into `MOCK_AUDIO.MA02`.
7. Change MA02 copy/status from browser-voice beta to production-first + labelled fallback.
8. Extend audio-manifest/mock-audio regression coverage.
9. Add a deployed production gate that verifies the exact four public files and playback mode.
10. Keep browser speech synthesis as fallback only.

## New-window handoff

If continuing in a new ChatGPT window:

- do not re-plan V1.12 audio preparation;
- read `MA02-PRODUCTION-AUDIO-PLAN.md` and the JSON spec;
- obtain the four canonical MP3 files;
- then continue directly with audio QA/integration/deployment.
