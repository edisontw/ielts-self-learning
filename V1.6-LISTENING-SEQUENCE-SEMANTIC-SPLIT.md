# V1.6 — Listening Sequence Semantic Split

Date: 2026-08-28
Status: **PRODUCTION-CLOSED**
Baseline main: `985f4f97dc8fefc8797be9ed51dddce0c36e41fe`
Semantic-split main: `2af534688a2cf90a758f070c34fd153331195b15`
Implementation main after deployed-QA hotfix: `e25f64eb04ef0d276b84420574b214c5c2a10f31`

## Goal

Resolve the final recurring `GAP-REVIEW` family without creating a frequency-driven LR02.

The previous semantics-first review found three questions under the historical umbrella tag `listening-sequence`:

- `ML02-Q4`
- `ML03-Q4`
- `ML04-Q3`

The three questions are not one coherent Listening skill.

## Question-level semantic split

| Question | Actual decision | New semantic tag | Teaching status |
| --- | --- | --- | --- |
| `ML02-Q4` | After passing the library, turn right immediately after the pharmacy | `listening-spatial-sequence` | QL03 already teaches continuous map/plan location tracking |
| `ML03-Q4` | After turning left at reception, locate Studio Five as the second door on the right after the kitchen | `listening-spatial-sequence` | QL03 already teaches continuous map/plan location tracking |
| `ML04-Q3` | After the morning break, Group A and Group B exchange sampling locations | `listening-procedural-sequence` | no exact existing owner; only one signal |

### Spatial route sequence — 2 signals

The two spatial questions require the learner to preserve an anchor and execute ordered directional updates. This is the same process QL03 teaches:

- fixed starting point;
- directional phrase;
- update current position;
- continue from the updated position.

However, there is still no equally exact Core lesson that forms a legitimate `Core → Lab` pair. L01 and L05 are prerequisites, not exact map-direction owners.

Decision:

`listening-spatial-sequence → TAUGHT-UNROUTED → QL03`

Because there are only two signals, no new LR Repair is justified.

### Procedural / event sequence — 1 signal

`ML04-Q3` is not map navigation. It asks what happens after a scheduled event in a field-project procedure.

Decision:

`listening-procedural-sequence → untreated but subthreshold`

One question cannot justify a new LR Repair.

## Implementation strategy — compatibility semantic retag

The original Mini Test question files retain their historical `listening-sequence` tag for backward compatibility. A dedicated semantic normalizer resolves the precise tag using stable question IDs:

- `ML02-Q4` → `listening-spatial-sequence`
- `ML03-Q4` → `listening-spatial-sequence`
- `ML04-Q3` → `listening-procedural-sequence`

This avoids destructive migration of existing learner records while making all new evidence precise.

The normalizer is applied in three places:

1. **Mini Test runtime** — item review and newly saved Error Notebook entries use the precise semantic tag.
2. **Mini Test trends** — fresh submissions are counted with precise tags; historical `missedErrorTags` using the old umbrella tag are normalized by Mini Test form (`ML02` / `ML03` / `ML04`).
3. **Error-tag / coverage audits** — canonical counts use the semantic tag rather than the historical source label.

Unknown legacy `listening-sequence` IDs are not guessed into a subtype.

## Validated recurrence consequence

Before split:

- `listening-sequence` = 3 signals → recurring `GAP-REVIEW`

After split:

- `listening-spatial-sequence` = 2 signals → QL03-taught, below recurring threshold
- `listening-procedural-sequence` = 1 signal → untreated, below recurring threshold

Validated recurring coverage matrix:

- V/G-REPAIR: **7 / 57**
- SKILL-REPAIR: **4 / 62**
- ROUTED-REUSE: **20 / 155**
- TAUGHT-UNROUTED: **11 / 41**
- GAP-REVIEW: **0 / 0 recurring families/questions**

The zero does **not** mean every possible Listening micro-skill now has a lesson. It means no remaining family with at least three semantically coherent Reading/Listening signals lacks an owner or safe treatment decision.

## Guardrails

- no LR02;
- no synthetic `L01/L05 → QL03` route;
- no route created from frequency alone;
- unknown legacy sequence IDs stay unsplit;
- historical Mini Test trend evidence is normalized without deleting history;
- no new localStorage key;
- no backup-schema change;
- no progress-denominator change;
- Mini Test Test Mode keeps no-single-question-Retry behavior.

## Deployed QA finding — Mini Test Error Notebook state handoff

The first production-origin sequence QA exposed a separate integration defect that pre-dated the semantic split:

1. Mini Test `Save missed items to Error Notebook` correctly wrote the new errors to `ielts-self-learning-v1`.
2. `app.js` retained the learner state loaded at startup in memory.
3. Unlike Full Mock, Mini Test did not have a handoff that refreshed the base app after an external core-state write.
4. A learner could therefore save Mini Test errors successfully but navigate to Improve and not see them until a manual page reload.

The production fix reuses the existing Full Mock handoff pattern:

- listen for `ielts-mini-test-errors-saved` as well as `ielts-mock-errors-saved`;
- mark the base core state as externally changed;
- reload once after Mini Test / Full Mock Exit;
- also reload on route/hash navigation after an external core write, covering sidebar navigation.

No storage key, backup schema, question scoring, Repair ownership, routing family, or progress denominator changed.

## Production closure evidence

### Semantic split implementation

- branch: `feat/v16-listening-sequence-semantic-split`
- PR: **#48 — V1.6 split Listening sequence semantics without LR02**
- PR head: `c491bda5eb6a8cd6057209fe57a36989d801c82e`
- PR Validate run: **33143219890** — PASS (`npm test` + browser smoke)
- semantic-split main: `2af534688a2cf90a758f070c34fd153331195b15`
- main Validate run: **33143300207** — PASS
- GitHub Pages run: **33143299595** — PASS

### Deployed-QA integration hotfix

- branch: `fix/v16-mini-test-error-handoff`
- PR: **#49 — Fix Mini Test Error Notebook state handoff**
- PR head: `125469518e9d7339ff24ea1ea3da768a8e26227c`
- PR Validate run: **33143849024** — PASS (`npm test` + browser smoke)
- implementation main after hotfix: `e25f64eb04ef0d276b84420574b214c5c2a10f31`
- main Validate run: **33143922701** — PASS
- GitHub Pages run: **33143921962** — PASS

### Production deployed E2E

- production origin: `https://edisontw.github.io/ielts-self-learning/`
- QA runner branch: `qa/v16-listening-sequence-split-deployed-e2e-hotfix`
- deployed E2E run: **33144022251** — **6 / 6 PASS**
- implementation main under test: `e25f64eb04ef0d276b84420574b214c5c2a10f31`
- artifact: `v16-listening-sequence-split-deployed-e2e`
- artifact ID: `9675071734`
- artifact files: **7**
- artifact size: **4,001,548 bytes**
- artifact SHA-256: `973df95291323ff31cfcb14c938ae182afc36f7a220ad180b5c48a7743e66522`

Deployed checks:

1. `ML02-Q4` actual Mini Test result and newly saved Error Notebook evidence use `listening-spatial-sequence`.
2. `ML04-Q3` actual Mini Test result and newly saved Error Notebook evidence use `listening-procedural-sequence`.
3. After Mini Test save/Exit, Improve immediately renders the saved Error Notebook entries; Mini Test errors retain the no-single-question-Retry boundary; no synthetic sequence reuse route and no LR02 appear.
4. QL03 renders successfully as the direct existing teaching owner for spatial route tracking.
5. Legacy ML02/ML03/ML04 history stored under the old umbrella `listening-sequence` normalizes to a **2/3-form `listening-spatial-sequence` recurrence only**; the single procedural signal is not falsely recurrent.
6. The semantic split and legacy normalization survive Learn → IELTS rerender without page errors.

## Next gate

There are now **zero recurring Reading/Listening `GAP-REVIEW` families** at the current recurrence threshold.

Do not create additional RR/LR units merely to increase Repair coverage. The next semantics-first review should move to still-unreviewed `TAUGHT-UNROUTED` three-count families and determine whether any non-retriable Test/Mock transfer item actually benefits from a precise existing-practice route:

- Listening `location-update` — 3, QL03-owned;
- Listening `word-fixation` — 3, L02-owned;
- Listening `word-limit` — 3, QL02/QL05-owned;
- Reading `mcq-scope` — 3, QR03-owned;
- Reading `topic-trap` — 3, QR05-owned.

Previously reviewed intentional deferrals remain closed unless new evidence changes their semantics or transfer need.
